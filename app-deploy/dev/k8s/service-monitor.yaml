apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: grpc-monitoring
  namespace: monitoring
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      monitoring: enabled
  namespaceSelector:
    any: true
  endpoints:
  - port: metrics
    interval: 15s
